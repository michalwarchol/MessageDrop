mutation GenerateNewCode($phoneOrEmail: String!) {
    generateNewCode(phoneOrEmail: $phoneOrEmail) {
        isOk
        errors {
            field
            message
        }
        redirect
    }
}