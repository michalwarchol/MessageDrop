fragment RegularMessage on Message {
    _id
    text
    roomId
    creatorId
    mediaId
    fileData {
        fileId
        filename
        mimeType
    }
    messageReactions {
        reaction
        value
    }
    createdAt
    updatedAt
}